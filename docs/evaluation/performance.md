# Performance — measured output

Generated by `PerformanceProbe`. Reproduce with:

```
mvn test -Dtest=PerformanceProbe
```

Median of 30 timed runs after 5 discarded warm-up runs.
Single machine, local SQL Server, no contention. These are indicative
of the intended deployment and are not a benchmark of the database.

| Operation | Median (ms) | Fastest (ms) | 95th percentile (ms) |
|---|---|---|---|
| Pooled connection acquisition | 0.97 | 0.55 | 1.55 |
| Count all published listings | 3.00 | 1.37 | 4.24 |
| Unfiltered page of 50 | 7.78 | 4.27 | 9.97 |
| Five-filter search, page of 50 | 2.06 | 1.52 | 2.93 |
| Every CLOSED property (the estimator's dataset) | 7.53 | 5.45 | 9.45 |
| Full table scan of property | 0.65 | 0.34 | 1.03 |

First connection of the session, which builds the pool: **787.4 ms**. Every subsequent acquisition is the first row of the table above — this is the cost pooling removes from each operation.
